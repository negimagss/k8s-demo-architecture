apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: local-test
spec:
  selector:
    app: nginx-local
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort
